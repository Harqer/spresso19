# Non-secret release configuration for the verified Spresso Convex deployment.
# Keep `.env.local` on the dev deployment; copy this value only into the release environment.
VITE_CONVEX_URL=https://woozy-anteater-572.convex.cloud
