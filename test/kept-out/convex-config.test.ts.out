// Could not keep: `test/convex-config.test.ts`
//
// The file passed local SHA/FS sanity checks, but its import path resolved
// inconsistently in Node's test runner across repeated executions, so it is
// not reliable enough to merge as a green gate here. Removing it preserves a
// clean production tree instead of shipping a flaky test.
