specVersion: "v1alpha"
serviceId: spresso-dataconnect
location: us-central1
schema:
  source: schema
  datasource:
    postgresql:
      database: spressodb
      cloudSql:
        instanceId: spresso-instance
connectorDirs: ["connector"]
