Security notice
===============

This file previously contained copied audit-chat output, including a live
credential. Secret values must never be stored in repository files or logs.

Spresso runtime credentials are referenced from Google Secret Manager. Use
functions/set_secrets.sh to validate that required secret resources and enabled
versions exist without reading or printing their values.

The credential that previously appeared here still requires coordinated history
removal and provider-side incident review; rewriting history or rotating keys is
intentionally outside this repository-only remediation.
